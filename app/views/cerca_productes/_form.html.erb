<%= simple_form_for(@cerca_producte) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :cerca %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
